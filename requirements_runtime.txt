# Production dependencies - only what the app needs to run

# Core RAG/LLM dependencies
langchain-core==0.3.74
langchain-groq==0.3.7
chromadb==1.0.16
sentence-transformers==5.1.0
transformers==4.55.0
torch==2.7.1
huggingface-hub==0.34.4

# Web application
gradio==5.42.0

# Core utilities
python-dotenv==1.1.1
tiktoken==0.11.0
pydantic==2.11.7

# EXCLUDED from runtime (build/dev only):
# requests==2.32.4          - Only for scraping scripts
# beautifulsoup4==4.13.4    - Only for web scraping
# lxml==6.0.0               - Only for HTML/XML parsing in scraping
# ratelimit==2.2.1          - Only for scraping rate limiting